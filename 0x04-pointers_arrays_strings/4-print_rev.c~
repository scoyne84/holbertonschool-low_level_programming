#include "holberton.h"

/**
 * print_rev - prints given string in reverse.
 * @s: string to be printed.
 * Return: void.
 */

void print_rev(char *s)
{
	int a = 127;

	while (s[a])
	{
		_putchar(s[a]);
		a--;
	}
	_putchar('\n');
}
