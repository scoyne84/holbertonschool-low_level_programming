#include "holberton.h"

/**
 * print_most_numbers - excludes 2 and 4 from printing.
 *
 * Return: void.
 */
void print_most_numbers(void)
{
	int x = ('0');

	for (x = '0'; x <= '9'; x++)
	{
		_putchar (x);
		if (x == '9')
		{
			_putchar ('\n');
		}
	}
}
