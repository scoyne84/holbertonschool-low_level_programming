#include "holberton.h"

/**
 * print_line - prints _.
 * @n: times line should print.
 * Return: void.
 */
void print_line(int n)
{
	if (n >= '1')
	{
		_putchar(_);
	}
	else
	{
		_putchar('\n');
	}
}
