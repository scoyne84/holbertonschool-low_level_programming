#include "holberton.h"

/**
 * main - check the code for Holberton School students.
 * @a: variable a
 * @b: variable b
 * Return: result.
 */
int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
