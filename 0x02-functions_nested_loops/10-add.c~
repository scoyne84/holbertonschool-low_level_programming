#include "holberton.h"
#include <stdio.h>

/**
 * main - check the code for Holberton School students.
 * @x: variable x for addition.
 * @y: variable y for addition.
 * Return: result.
 */
int add(int x, int y)
{
	int result;

	result = (x + y)

	return (result);
}
